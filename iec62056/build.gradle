group 'de.offis.estream'
version LIBRARY_VERSION

ext {
    jackson_version = '2.9.0'
}

apply plugin: 'java-library'
apply plugin: 'osgi'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    api "com.fasterxml.jackson.core:jackson-annotations:$jackson_version"
}
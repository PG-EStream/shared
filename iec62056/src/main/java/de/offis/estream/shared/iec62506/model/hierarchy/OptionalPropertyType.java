package de.offis.estream.shared.iec62506.model.hierarchy;

public enum OptionalPropertyType {
    // add property types here
    // lower case because this will also be the prop name in Typescript
    location,
    options,
    tags;
}
